<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:topLeftRadius="@dimen/widget_corners_size"
        android:topRightRadius="@dimen/widget_corners_size"/>
    <stroke
        android:width="1dp"
        android:color="@color/colorPrimaryTransparent">
    </stroke>
    <solid android:color="@color/colorBackgroundLight"/>
</shape>
